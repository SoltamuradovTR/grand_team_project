const initialState = {
  signingUp: false,
  signingIn: false,
  error: null,
  editingAgent: null,
  loading: false
};

const agent = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default agent;

